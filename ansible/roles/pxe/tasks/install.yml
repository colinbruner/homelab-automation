---

  # NOTE: Installing both pxelinux / ipxe packages
  # if only relying on 1, this can be tweaked
- name: Install required packages
  ansible.builtin.apt:
    pkg: "{{ pxe_packages + pxe_ipxe_packages }}"
    update_cache: true
