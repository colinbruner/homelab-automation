---
ansible_user: root

proxmox_cloudflare_token: "{{ lookup('community.general.onepassword', 'cloudflare-proxmox', field='acme-token', vault='semaphore') }}"
proxmox_cloudflare_account_id: "{{ lookup('community.general.onepassword', 'cloudflare-proxmox', field='account-id', vault='semaphore') }}"
proxmox_acme_contact: "{{ lookup('community.general.onepassword', 'cloudflare-proxmox', field='acme-email', vault='semaphore') }}"
proxmox_oidc_issuer_url: "{{ lookup('community.general.onepassword', 'pocket-id-proxmox', field='issuer-url', vault='semaphore') }}"
proxmox_oidc_client_id: "{{ lookup('community.general.onepassword', 'pocket-id-proxmox', field='client-id', vault='semaphore') }}"
proxmox_oidc_client_secret: "{{ lookup('community.general.onepassword', 'pocket-id-proxmox', field='client-secret', vault='semaphore') }}"
